/**
 * Route shell for /dashboard/crew.
 *
 * The real UI is keep-alive mounted from the dashboard layout so navigating
 * away and back does not remount (and "reload") the dossier.
 */
export default function CrewRoutePage() {
  return null;
}
