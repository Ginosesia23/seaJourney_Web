/**
 * Shared brand color for all transactional emails (invite, welcome, subscription, etc.).
 * Matches the dark blue used in the forgot-password / reset flow and keeps emails consistent.
 */
export const EMAIL_PRIMARY_BLUE = '#172b42';
